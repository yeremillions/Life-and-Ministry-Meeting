# Supabase telemetry (optional — heartbeat + admin page)
# Create a free project at https://supabase.com, then:
#   1. Create a table called "heartbeats" (see README for SQL)
#   2. Copy your project URL and anon key here
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Admin page PIN (defaults to "admin123" if not set)
VITE_ADMIN_PIN=
